//let age; // not given a value so it is given a value of undefined by the browser

//console.log(age, age + 3, `the age is ${age}`);

let age = null; // if we want to redefine user input after they have entered values but then want to reset them

console.log(age, age + 3, `the age is ${age}`);
